#ifndef CONFIG_H
#define CONFIG_H

#include "settings.h"

/**
 * @file globalConfig.h
 * @brief Placeholder for compile‑time feature flags or board specific
 *        configuration.  Currently empty but kept for compatibility with
 *        existing modules that include it.
 */

#ifdef BASIC
#endif // BASIC

#endif // CONFIG_H
